import LiveSupportPage from "@/features/live-support/LiveSupportPage";

export const metadata = {
  title: "Live Support — 4AM.WAV",
  robots: "noindex, nofollow",
};

export default function Page() {
  return <LiveSupportPage />;
}
